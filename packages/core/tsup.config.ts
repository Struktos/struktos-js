// ============================================================================
// @struktos/core - tsup Build Configuration
// ============================================================================
//
// 🎯 Purpose:
// Configures tsup (esbuild-powered) bundler for @struktos/core.
// Outputs both CJS and ESM formats with TypeScript declarations.
//
// 🏛️ ADaaS Vision Alignment:
// - Dual-format output for maximum compatibility
// - Tree-shakeable ESM for modern bundlers
// - Efficient build times via esbuild
//
// ============================================================================

import { defineConfig } from 'tsup';

export default defineConfig({
  // ─────────────────────────────────────────────────────────────────────────
  // Entry Points
  // Maps to the package.json exports field
  // ─────────────────────────────────────────────────────────────────────────
  // entry: {
  //   index: 'src/index.ts',
  //   // eslint-disable-next-line @typescript-eslint/naming-convention
  //   'domain/index': 'src/domain/index.ts',
  //   // eslint-disable-next-line @typescript-eslint/naming-convention
  //   'application/index': 'src/application/index.ts',
  //   // eslint-disable-next-line @typescript-eslint/naming-convention
  //   'infrastructure/index': 'src/infrastructure/index.ts',
  // },
  entry: ['src/index.ts'],

  // ─────────────────────────────────────────────────────────────────────────
  // Output Configuration
  // ─────────────────────────────────────────────────────────────────────────
  format: ['cjs', 'esm'],
  dts: {
    resolve: true,
    entry: 'src/index.ts',
  },
  splitting: true,
  sourcemap: true,
  clean: true,
  treeshake: true,
  minify: false,

  // ─────────────────────────────────────────────────────────────────────────
  // Target & External
  // ─────────────────────────────────────────────────────────────────────────
  target: 'node18',
  outDir: 'dist',

  // External packages (peer dependencies)
  external: [],

  // ─────────────────────────────────────────────────────────────────────────
  // Banner (License Header)
  // ─────────────────────────────────────────────────────────────────────────
  esbuildOptions(options) {
    options.banner = {
      js: `/**
 * @struktos/core
 * @license Apache-2.0
 * @copyright Struktos.js Team
 */`,
    };
  },
});
